To view the assignment guidelines, install Jupyter Notebook:

https://jupyter.org/install

In the command line, navigate to the root directory of the project and type "jupyter notebook". Then open "PROJECT1_GUIDELINES.ipynb" in the resulting display.